// Owned by the release process — see CLAUDE.md "Never touch". Do not edit
// this file from a feature branch; real values are injected at deploy time.
export const environment = {
  production: true,
  apiBaseUrl: '__INJECTED_AT_DEPLOY__',
};
