{
  "name": "workshop-3-sample",
  "version": "1.0.0",
  "private": true,
  "description": "Workshop 3 sample repo (Technical Lead & Team Lead track) - NAICO ITS",
  "scripts": {
    "test": "jest",
    "lint": "echo '(lint stub) wire up ESLint here for a real project' && exit 0",
    "build": "echo '(build stub) wire up ng build / your real build step here' && exit 0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
